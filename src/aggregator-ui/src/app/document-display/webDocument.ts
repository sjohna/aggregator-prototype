// TODO: change the name of this class. I was going to call it Document, but that's already a type, and that's probably too confusing
export class WebDocument
{
    id?: number;
    title?: string;
    updateTime?: Date;
    sourceLink?: string;
    sourceID?: string;
    content?: string;
    publishTime?: Date;
}
